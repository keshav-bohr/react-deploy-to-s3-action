name: "React Deploy to S3 using latest node"
description: "Build a React.js web app and sync to an AWS S3 repository"
author: keshav-bohr
runs:
  using: docker
  image: Dockerfile
branding:
  icon: upload-cloud
  color: green
